{
  "name": "grocery-store-backend",
  "version": "1.0.0",
  "description": "Backend API for Fresh Grocery Store application",
  "main": "server.js",
  "scripts": {
    "start": "nodemon backend/server.js",
    "dev": "nodemon backend/server.js",
    "build":"npm install && npm install --prefix frontend && npm run build --prefix frontend",

    "seed": "node seed.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["grocery", "store", "api", "express", "mongodb"],
  "author": "Grocery Store Team",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
